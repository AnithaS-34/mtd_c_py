#include<stdio.h>
#include<math.h>
int main()
{
    int a=0;
    puts("Enter a number to check if it is a perfect square");
    scanf("%d",&a0);
    int root=floor(sqrt(a));
    if(root*root==a)
    printf("%d is a perfect square",a);
    elseprintf("%d is not a perfect square root");
    return 0;
}
